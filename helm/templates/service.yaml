apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-backend.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ include "warehouse-backend.name" . }}